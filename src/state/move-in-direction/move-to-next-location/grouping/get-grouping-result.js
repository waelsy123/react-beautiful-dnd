// @flow
import type { Args, Result } from '../move-to-next-location-types';

export default ({
  isMovingForward,
  critical,
  previousPageBorderBoxCenter,
  previousImpact,
  droppable,
  draggables,
  viewport,
}: Args): ?Result => null;
